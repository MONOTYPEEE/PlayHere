import { makeStyles } from "@fluentui/react-components";

export const QueueCardStyle = makeStyles({
    e2e:{
        display: 'flex',
        justifyContent:'space-between',
        alignItems: 'center',
    }
})