import { makeStyles } from "@fluentui/react-components";

export const ControllerCardStyle = makeStyles({
    buttonContainer:{
        display: 'flex',
        flexDirection: 'row',
        columnGap: '8px'
    }
})