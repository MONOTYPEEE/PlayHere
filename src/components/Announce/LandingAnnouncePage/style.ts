import { makeStyles } from "@fluentui/react-components";

export const AnnouncementStyle = makeStyles({
    root:{
        display: 'flex',
        justifyContent: 'center',
        paddingTop: '75px'
    },
    clild:{
        maxWidth: '1108px',
        width: '-webkit-fill-available',
        display: 'flex',
        flexDirection: 'column',
    },
});