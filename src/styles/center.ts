import { makeStyles } from "@fluentui/react-components";

export const Center = makeStyles({
    flex:{
        display:'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100vh '
    }
})